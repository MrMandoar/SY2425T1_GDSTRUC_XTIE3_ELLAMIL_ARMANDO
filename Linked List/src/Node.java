// da nodes, which is also da elements
class Node {
    String data;  // Character name
    Node next;    // pointer por next node

    public Node(String data) {
        this.data = data;
        this.next = null;
    }
}
